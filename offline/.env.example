# Salesforce instance the Vite proxy forwards /services to.
VITE_SF_INSTANCE_URL=https://zetapharma.my.salesforce.com

# Optional. Connected App access token. If empty, the PWA shows a token form.
# Store a local copy in offline/.env.local (gitignored).
VITE_SF_ACCESS_TOKEN=
